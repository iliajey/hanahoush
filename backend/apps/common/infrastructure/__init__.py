"""Infrastructure layer of the Common app.

Concrete implementations of the ports defined in the domain layer
(Django ORM models, repositories, external gateways). Only this layer
may import Django.
"""
